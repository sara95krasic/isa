import { SortPozBioPipe } from './sort-poz-bio.pipe';

describe('SortPozBioPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPozBioPipe();
    expect(pipe).toBeTruthy();
  });
});
